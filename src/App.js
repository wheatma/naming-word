import React, { PureComponent } from 'react';
import csvParse from './lib/csvParse';

export default class App extends PureComponent {
    async componentDidMount() {
    }
    render() {
        return <div>naming word</div>
    }
}